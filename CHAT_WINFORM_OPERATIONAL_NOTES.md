# סיכום אופרטיבי: WinForms + צ׳אט AI (מפרויקט WinForm_6)

מסמך זה נועד **ליישם מחדש** אפליקציית צ׳אט עם מודלי בינה ב־WinForms **בלי לחזור על אותן מלכודות**, על בסיס התהליך שעבר ב־`WinForm_6`.

---

## 1. מטרות מוצר ו־UX

- **עברית / RTL**: הטופס ב־`RightToLeft` + `RightToLeftLayout`; בתוך אזור הצ׳אט לעיתים `RightToLeft = No` על מיכלים פנימיים כדי שלא “ייתהפכו” פעמיים יישור ויזואלי.
- **זרימת ווטסאפ**: הודעת משתמש בצד אחד, תשובת AI בצד השני — **זרם אחד**, בלי כותרות “יוזר / AI” בתוך אזור השיחה (אם נשארו `Label`־ים בדיזיינר — להסתיר או גובה שורה 0).
- **בחירת מודל + System Prompt + שלח / נקה** — פונקציונליות בסיסית שחוזרת בכל גרסה.

**להימנע:** שילוב `FlowLayoutPanel` / `AutoSize` כבד על גלילה — גורם לריקים, גבהים 0, ובועות שלא נראות.

---

## 2. אינטגרציה עם API

- **מפתחות**: טעינה מ־`.env` בתחילת הריצה (`DotNetEnv.Env.TraversePath().Load()` ב־`Program.cs`).
- **שכבת SDK**: שימוש במחלקות ייעודיות (למשל `OpenAI_SDK_Response`, `Gemini_SDK`) — לא לשכפל לוגיקת HTTP בתוך הטופס.
- **חוסן**: בדיקת מפתח / null בזרימת קריאה; ב־Gemini — בדיקות ל־`Candidates` / `Parts` לפני גישה.

**להימנע:** להניח שתמיד יש טקסט ב־streaming או במבנה קבוע — תמיד fallback ל־`string.Empty`.

---

## 3. הצגת תשובת המודל (החלטה מוצרית חשובה)

- ב־WinForm_6 הוחלט: **אין הזרקת צ׳אנקים בממשק** — מוצגת **התשובה המלאה** אחרי שהקריאה מסתיימת (`Call` אחד), עם בועת “חשיבה” בינתיים.

**תוצאה צפויה:** פחות ריצוד, פחות פריסות ביניים, פחות באגי גלילה — במחיר המתנה עד סוף התשובה.

**להימנע:** עדכון UI על כל צ׳אנק בלי שליטה בפריסה ובגלילה — ב־WinForms זה מקור קבוע לבאגים.

---

## 4. בועות דיבור וטקסט עברי

- **`SpeechBubble`**: בועה עם זנב, צבעי משתמש/AI, `MaximumSize` לרוחב מקסימלי יחסי.
- **`BubbleTextView`**: ציור טקסט עם `TextRenderer` — **יישור RTL**, עטיפה, ורקע **אטום** (`Opaque` + מילוי לפני ציור).

**תובנה קריטית (ריצוד / “צללית” טקסט):**  
אל לשלב `TextFormatFlags.VerticalCenter` עם `WordBreak` על פסקאות — יוצר ארטיפקטים. עדיף: **מדידת גובה הבלוק** ואז ציור במלבן **ממורכז אנכית** (חישוב `y` ידני).

**להימנע:** `BackColor = Transparent` על שכבת טקסט עם ClearType מעל רקע שמתעדכן כל הזמן.

---

## 5. אינדיקטור “חשיבה” (נקודות)

- אנימציה עם `Timer` + `Invalidate` **רק על הפקד עצמו**.
- **לא** לקרוא `Parent.Invalidate` בכל טיק — גורם לריצוד של כל הבועה.

**להימנע:** שקיפות מיותרת אם אפשר צבע זהה לרקע הבועה.

---

## 6. גלילה — הלקח המרכזי של WinForm_6

### מה מביא לתוצאה יציבה

- **`AutoScroll = false`** על פאנל אזור הצ׳אט.
- פאנל פנימי **`_chatHost`** שמחזיק את כל שורות ההודעות, בגובה = סכום התוכן + padding תחתון.
- **גלילה ידנית**: `_scrollY` + מיקום `_chatHost` ב־`(0, -_scrollY)`.
- **גלגלת עכבר**: `Application.AddMessageFilter` + בדיקה שהסמן מעל אזור הצ׳אט (כי `Panel` לא תמיד מקבל Wheel).

**תוצאה צפויה:** בלי פסי גלילה של המערכת שנלחמים ב־`ShowScrollBar`, בלי “קפיצות” מעצבנות, בלי לאבד שליטה על טווח הגלילה.

### מה לעשות כשחושבים על “להסתיר סקרול”

- `ShowScrollBar` + `AutoScroll` הם **נלחמים בפריסה** — הפס חוזר אחרי `Layout`.
- **לא** לסמוך על הסתרה ויזואלית בלבד כפתרון ארוך טווח.

**להימנע:** לולאות `HideChatScrollbar` בכל `Paint` — עלול להחמיר ריצוד.

---

## 7. מניעת “הכול נעלם” / גלילה שבורה

- אם **`ClientSize.Height <= 0`** (רגע מעבר חלון, פריסה, מזעור):  
  - `GetMaxScrollY()` חייב להחזיר **0** (לא `גובה_תוכן - 0` = כל התוכן).
  - לא לעדכן `_chatHost.Top` כשאין viewport תקף.
- **מעבר ראשון ל־overflow** (תוכן גבוה מגובה הצפייה): לעיתים פריים אחד עם גבולות “לא יבשים” — עוזר **BeginInvoke** אחד ל־`ApplyChatHostLayout` + `Invalidate` על ה־host (פעם אחת למצב overflow).

**להימנע:** `ScrollControlIntoView` / `AutoScrollPosition` כמקור אמת יחיד — ב־WinForms זה יציב פחות מהגלילה הידנית כאן.

---

## 8. רוחב הבועה הראשונה / מדידה מוקדמת

- למדוד טקסט **אחרי** שהשורה והבועה כבר בתוך `_chatHost` (הורה אמיתי).
- **`GetChatViewportWidth()`**: אם `ClientWidth == 0`, fallback ל־`DisplayRectangle` / רוחב `pnlChatContainer` פחות padding — **לא** ליפול ל־200px “בטוח” בלבד בלי הקשר.

**תוצאה צפויה:** בועה ראשונה צמודה לתוכן כמו השאר.

---

## 9. גבהים ומדידות — הגנה מפני ערכי ענק

- לעיתים `MeasureText` יכול להחזיר גבוה חריג — מפיל את טווח הגלילה (“ריק ענק מתחת להודעות”).
- לכן: **תקרה סבירה** לגובה מדידה (בבועה / בשורה / ב־`BubbleTextView`).

**להימנע:** להסתמך על מדידה בלי תקרה כשהטקסט ארוך מאוד.

---

## 10. זנב הבועה (שורה אחת מול רב־שורות)

- `TailOffsetY` שונה לבועת **שורה אחת** לעומת **רב־שורות** — מבוסס על `BubbleTextView.IsSingleLine`.
- זה עיצובי; חשוב לעדכן `Invalidate` על הבועה אחרי שינוי זנב.

---

## 11. פקדים מותאמים אישית והדיזיינר (WFO1000)

- לפקדים מותאמים: `[DefaultValue]`, `Reset*` / `ShouldSerialize*` או `[Browsable(false)]` + `DesignerSerializationVisibility(Hidden)` לשדות שלא צריכים להישמר בקוד גנרטיבי.

**תוצאה צפויה:** הדיזיינר והבנייה לא נשברים.

---

## 12. בנייה והרצה

- אם `MSB3026` (קובץ נעול): לסגור תהליך `WinForm_6.exe` לפני `dotnet build`.

---

## צ׳ק־ליסט קצר לפני “סיימתי”

| נושא | בדיקה |
|------|--------|
| RTL | טופס + טקסט בועות קריא |
| צ׳אט | זרם אחד, בלי כותרות מפוצלות מיותרות |
| גלילה | ידנית עם `_chatHost`, לא `AutoScroll` |
| viewport | לא לגלול כשגובה 0 |
| תשובה | מלאה או סטרימינג — החלטה מוצרית מודעת |
| טקסט | אטום, מרכוז אנכי חישובי, לא VerticalCenter+WordBreak |
| מדידות | תקרות גובה, רוחב viewport אמיתי |
| מפתחות | `.env` + שגיאות ברורות אם חסר |

---

*מסמך זה משקף את סט ההחלטות והתובנות מהפרויקט WinForm_6; בעת העתקה לפרויקט חדש — להתאים שמות פקדים ומבנה דיזיינר, אבל **עקרונות הגלילה והטקסט** נשארים רלוונטיים.*
